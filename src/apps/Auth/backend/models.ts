export type AuthCredentials = {
    username: string;
    password: string;
};

export type Credentials = {
    name: string;
    description: string;
};

export type User = {
    username: string;
};
